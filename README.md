# Agent Observability Index (by Panshi)

Neutral directory of AI agent observability / evals / guardrails / prompt-mgmt / cost / debugging tools.

- `data/tools.json` — 77 entries, manually verified against primary sources (June 2026)
- `build.py` — zero-dependency static site generator → `docs/` (GitHub Pages)
- Rebuild: `python3 build.py && rm -rf docs && cp -r site docs`
